# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`travtools` is a tooling project for the Traveller tabletop RPG (2022 Core Rulebook edition). The reference rulebook is at `docs/Traveller 2022 Core Rulebook 20-02-2026.pdf`.

## Repository Structure

- `apps/` — standalone applications (CLI tools, web apps, etc.)
- `src/` — shared library code consumed by apps
- `test/` — tests
- `docs/` — reference material (Traveller 2022 Core Rulebook PDF)

The project has no code yet. When adding the first app or library, establish the language/runtime choice and update this file with build, lint, and test commands.
